#include <iostream>
#include <unistd.h>
using namespace std;

int main() {
    pid_t pid = fork();

    if (pid == 0) {
        cout << "CHILD: Running child-specific code\n";
    } else {
        cout << "PARENT: Running parent-specific code\n";
    }
    return 0;
}
